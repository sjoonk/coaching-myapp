var markup = '<ul id="recent-entries">';

<% @entries.each do |entry| %>
markup += '<li><%= entry.title %> - ';
markup += '<%= entry.created_at.strftime("%Y-%m-%d") %></li>';
<% end %>
markup += '</ul>';

document.write(markup);
